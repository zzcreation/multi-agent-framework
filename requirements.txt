# Core dependencies (Python standard library only)
# No external dependencies required for core functionality

# Optional dependencies
# Note: Using latest stable versions to avoid known vulnerabilities
flask>=3.0.0
psutil>=6.0.0